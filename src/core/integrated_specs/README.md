# Integrated Canonical Specifications

This directory contains specification modules that were copied into the tree.

They are Python files that exist. They are **not** evidence that a civilization-tier
runtime ships. `coverage.json` still omits this package. Measured status:
[CLAIMS_AUDIT.md](../../../CLAIMS_AUDIT.md).

**Removability**: This directory can be removed if the prototype should run without it.
