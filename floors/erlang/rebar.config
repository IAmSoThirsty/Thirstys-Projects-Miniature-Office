{erl_opts, [debug_info, warnings_as_errors]}.

{deps, [
    {jsx, "3.1.0"}
]}.

{escript_incl_apps, [department_floor, jsx]}.
{escript_main_app, department_floor}.
{escript_name, department_floor}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

{profiles, [
    {prod, [
        {erl_opts, [no_debug_info, warnings_as_errors]}
    ]}
]}.
